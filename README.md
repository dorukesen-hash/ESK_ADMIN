# ESK_ADMIN

Standalone admin panel for the ESK Packaging e-commerce platform. Talks to the `ESK_API` backend; independent from the `ESK_FE` storefront's codebase, sharing only its brand visual language (colors, typography, control shapes).
